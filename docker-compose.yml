services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file: .env
    environment:
      # Dentro del contenedor, localhost no llega al host; se usa host.docker.internal
      - POSTGRES_HOST=host.docker.internal
      # Puerto del túnel SSH que expone la DB externa en el host
      - POSTGRES_PORT=5433
    volumes:
      - ./backend:/app
    # Permite al contenedor alcanzar servicios del host (PostgreSQL, n8n, WPPConnect)
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    env_file: .env
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next
    depends_on:
      - backend
    restart: unless-stopped
